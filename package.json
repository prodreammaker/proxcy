{
  "name": "kv-data-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler-kv-dashboard.toml",
    "deploy": "wrangler deploy -c wrangler-kv-dashboard.toml",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8",
    "@eslint/js": "^9.39.4",
    "eslint": "^9",
    "vitest": "^3",
    "wrangler": "^4"
  }
}
